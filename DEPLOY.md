# COGITO.ART - Investment Hub

React + TypeScript + Supabase проект для систематической торговли и инвестиций.

## 🚀 Деплой на Vercel

Проект настроен для простого деплоя на Vercel - идеальной платформе для React приложений.

### Быстрый старт

#### Вариант 1: Через веб-интерфейс Vercel (Рекомендуется)

1. **Зарегистрируйтесь на Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Создайте бесплатный аккаунт

2. **Импортируйте проект:**
   - Нажмите "Add New Project"
   - Выберите "Import Git Repository" ИЛИ загрузите папку проекта напрямую
   - Vercel автоматически определит настройки для Vite

3. **Настройте переменные окружения:**
   - В разделе "Environment Variables" добавьте:
     - `VITE_SUPABASE_URL` - URL вашего Supabase проекта
     - `VITE_SUPABASE_ANON_KEY` - Anon ключ от Supabase
     - `GEMINI_API_KEY` - API ключ Gemini (если используется)

4. **Нажмите Deploy:**
   - Vercel автоматически соберет и опубликует ваш проект
   - После деплоя вы получите уникальный URL

#### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Запустите деплой
vercel

# Для продакшн деплоя
vercel --prod
```

### После деплоя

Ваш сайт будет доступен по адресу:
```
https://your-project-name.vercel.app
```

Вы также можете привязать свой собственный домен в настройках Vercel.

### Локальная разработка

```bash
# Установите зависимости
npm install

# Создайте .env файл с вашими переменными
cp .env.example .env

# Запустите dev сервер
npm run dev
```

Сайт будет доступен на `http://localhost:3000`

### Технические детали

- **Platform**: Vercel
- **Framework**: Vite + React + TypeScript
- **Database**: Supabase PostgreSQL с RLS
- **Auth**: Supabase Auth (email/password)
- **Configuration**: `vercel.json` настроен для SPA роутинга
- **Build Command**: `npm run build`
- **Output Directory**: `dist`

### Альтернативные платформы

Проект также совместим с:
- **Netlify** - импортируйте проект и настройте переменные окружения
- **Railway** - деплой через веб-интерфейс или CLI
- **Render** - создайте Static Site и укажите build команду

### Автоматические обновления

При использовании Git репозитория, Vercel автоматически деплоит изменения:
- Каждый push в main ветку создает продакшн деплой
- Pull requests получают preview URL для тестирования

### Troubleshooting

**Проблема: 404 на внутренних страницах**
- Решение: Проверьте наличие `vercel.json` с настройкой rewrites

**Проблема: Переменные окружения не работают**
- Решение: Убедитесь, что переменные начинаются с `VITE_` и добавлены в настройках Vercel

**Проблема: Build failed**
- Решение: Запустите `npm run build` локально, чтобы увидеть ошибки
